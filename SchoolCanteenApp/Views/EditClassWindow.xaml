<Window x:Class="SchoolCanteenApp.Views.EditClassWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактировать класс" 
        Height="250" 
        Width="400"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Название класса:" Margin="0 0 0 5"/>
        <TextBox Grid.Row="1" Text="{Binding Class1}" Margin="0 0 0 10"/>

        <TextBlock Grid.Row="2" Text="Классный руководитель:" Margin="0 0 0 5"/>
        <ComboBox Grid.Row="3"
                  x:Name="TeacherComboBox"
                  DisplayMemberPath="LastName"
                  SelectedValuePath="IdTeacher"
                  SelectedValue="{Binding IdTeacher}"
                  Margin="0 0 0 15"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Сохранить" Width="80" Click="SaveButton_Click" Margin="0 0 10 0"/>
            <Button Content="Отмена" Width="80" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>